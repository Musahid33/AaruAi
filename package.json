{
  "name": "aaru-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Aaru AI — Personal AI Studio. Zero-dependency Node.js (optional PostgreSQL / Firebase Firestore).",
  "main": "server.min.js",
  "scripts": {
    "start": "node server.min.js",
    "dev": "node src/server.js",
    "build": "node scripts/build.js",
    "test": "node scripts/e2e.js"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "firebase-admin": "^12.0.0"
  },
  "engines": { "node": ">=18" }
}
